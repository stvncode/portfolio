import { createStyles } from '@mantine/core'

export const useContactStyles = createStyles({
  form: {
    width: '400px',
    display: 'flex',
    flexDirection: 'column',
  },
  input: {
    fontSize: '2rem',
  },
})
